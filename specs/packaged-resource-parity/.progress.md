---
spec: packaged-resource-parity
basePath: ./specs/packaged-resource-parity
phase: planned
task: 0/0
updated: 2026-07-02T12:54:35.201Z
---

# Progress: packaged-resource-parity

## Original Goal
As a Pi Smart Ralph maintainer, I want the package to ship the original workflow resources and compatibility schemas, so that commands, agents, docs, and tests share one auditable contract.

## Epic Context
- Epic: smart-ralph-parity-audit
- Epic state: ./specs/_epics/smart-ralph-parity-audit/.epic-state.json
- Dependencies: none

## Completed Tasks
- [x] 1.1 [RED] Failing verifier: manifest file must exist and be an array - db5fc56

## Current Task
Planned from epic triage. Run /ralph-start to begin normal Ralph phases.

## Learnings
_Discoveries and insights will be captured here_

## Blockers
- None currently

## Next
Run /ralph-start packaged-resource-parity

## Learnings
- npm packaging should be verified with both `npm run prepack` and `npm pack --dry-run --json`; npm docs define `files` as the publish allowlist and `npm pack --dry-run` as publish-content preview — source: https://docs.npmjs.com/cli/v10/configuring-npm/package-json#files, https://docs.npmjs.com/cli/v10/commands/npm-pack
- Existing Pi package resource roots `templates/`, `references/`, `skills/`, and `prompts/` are present but only `.gitkeep`/empty, and no `schemas/` directory exists — source: package resource directories
- Original Ralph Specum resources are available locally under `/home/nephy/pi-custom-workflow/smart-ralph/plugins/ralph-specum` and include commands, templates, references, skills, and `schemas/spec.schema.json` — source: `/home/nephy/pi-custom-workflow/smart-ralph/plugins/ralph-specum`
- `LICENSE` and `smart-ralph.png` are listed in `package.json.files` but absent, so requirements need an explicit add-or-remove packaging decision — source: `package.json` and filesystem check
- No lint/typecheck/test/build scripts, Makefile targets, CI run commands, browser E2E dependencies/configs, or MCP browser/devtools/database tools were detected; verification should focus on `npm run prepack` and pack dry-run until tests are added — source: `package.json`, repo quality-command discovery, MCP lazy searches

### Research phase (2026-07-02T13:11:49.184Z)
- Generated research.md and validated coordinator output.
- Approval decision: changes_requested.
- Awaiting approval boundary. Next command: /ralph-requirements.

## Learnings
- Requirement insight: `RalphResourceManifestV1` must inventory every original command/template/reference/skill/schema file with a finite status and notes for non-exact parity.
- Requirement insight: Package verification should validate both repository files via `npm run prepack` and tarball inclusion/exclusion via `npm pack --dry-run --json`.
- Scope decision: Original commands and hooks are manifest inventory inputs only; this spec must not reintroduce Claude/Codex hook execution or implement missing command behavior.
- Scope decision: `LICENSE` and `smart-ralph.png` require an explicit add/remove packaging decision before implementation completion.

### Requirements phase (2026-07-02T15:29:18.042Z)
- Generated requirements.md and validated coordinator output.
- Approval decision: approved.
- Awaiting approval boundary. Next command: /ralph-design.

### Design phase (2026-07-02T15:45:00Z)
- Generated design.md with manifest, resource packaging, prepack verification, dry-run verification, and documentation architecture.
- Approval decision: pending.
- Awaiting approval boundary. Next command: /ralph-tasks after design approval.

## Learnings
- Architecture decision: use `references/ralph-resource-manifest.v1.json` as a top-level array because it directly matches the epic `RalphResourceManifestV1` entry contract and is already package-included.
- Architecture decision: keep original commands under `references/original-commands/` because this preserves auditability without making Claude/Codex command markdown executable in Pi.
- Architecture decision: keep `npm pack --dry-run --json` validation in a separate script because invoking pack from `prepack` would risk recursive lifecycle execution.
- Existing pattern to follow: `scripts/verify-publish-bundle.mjs` aggregates failures under `Smart Ralph package verification failed:` and already enforces Pi manifest, agent, model-frontmatter, and bundled dependency checks.
- Existing pattern to follow: `README.md` already has Package layout, Local development, and Publishing sections where resource roots and verification commands should be documented.

### Design phase (2026-07-02T15:35:45.347Z)
- Generated design.md and validated coordinator output.
- Approval decision: approved.
- Awaiting approval boundary. Next command: /ralph-tasks.

### Tasks phase (2026-07-03)
- Generated tasks.md with MID_SIZED TDD Red-Green-Yellow plan, package dry-run VE checks, and final quality gates.

## Learnings
- Planning decision: infer MID_SIZED workflow because Intent Classification is absent and this is an existing package/resource verification change rather than GREENFIELD work.
- Planning decision: resolve absent `LICENSE` and `smart-ralph.png` by removing them from `package.json.files` rather than inventing unapproved assets.
- Verification command/tool: `npm run prepack` / research.md Quality Commands and Verification Tooling.
- Verification command/tool: `npm pack --dry-run --json` / research.md package dry-run and npm docs source.

### Tasks phase (2026-07-02T16:35:32.289Z)
- Generated tasks.md and validated coordinator output.
- Approval decision: approved.
- Awaiting approval boundary. Next command: /ralph-implement.

## Learnings

### Verification: 0.1 Establish package verification baseline
- Status: PASS
- Commands/Tools: `npm run prepack`(0)
- Evidence: Existing package verifier completed successfully: `node scripts/verify-publish-bundle.mjs` ran with no `Smart Ralph package verification failed:` output; no pre-existing failures observed.

### Implementation task 1: [VERIFY] 0.1 Establish package verification baseline (2026-07-02T16:37:11.996Z)
- Agent: ralph-qa-engineer
- Signal: VERIFICATION_PASS
- Native task: #1
- Evidence: npm run prepack: PASS exited 0; verifier ran with no failure output

### Implementation task 1.1: [RED] Failing verifier: manifest file must exist and be an array (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS`
- Evidence: RED_PASS; verifier now reports aggregate failure when `references/ralph-resource-manifest.v1.json` is missing.

## Current Task
Awaiting next task

## Learnings
- Manifest bootstrap starts as a RED prepack verifier check: missing `references/ralph-resource-manifest.v1.json` now fails under the existing aggregate `Smart Ralph package verification failed:` report.

### Implementation task 2: 1.1 [RED] Failing verifier: manifest file must exist and be an array (2026-07-02T16:38:44.640Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #2
- Evidence: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS` output `RED_PASS`

### Implementation task 1.2: [GREEN] Pass manifest presence check with an empty fixture (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run prepack`
- Evidence: prepack exited 0 after adding `references/ralph-resource-manifest.v1.json` as `[]`.

## Completed Tasks
- [x] 1.2 [GREEN] Pass manifest presence check with an empty fixture - a997c73

## Current Task
Awaiting next task

## Learnings
- An empty top-level JSON array satisfies the current `RalphResourceManifestV1` bootstrap contract and allows the prepack manifest presence/array-shape check to pass.

### Implementation task 3: 1.2 [GREEN] Pass manifest presence check with an empty fixture (2026-07-02T16:40:22.329Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #3
- Evidence: `npm run prepack` exited 0 after adding empty manifest fixture.

### Implementation task 4: 1.3 [YELLOW] Refactor manifest verifier helpers (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run prepack`
- Evidence: prepack exited 0 after extracting manifest path constants and JSON parsing helper while preserving manifest path diagnostics.

## Completed Tasks
- [x] 1.3 [YELLOW] Refactor manifest verifier helpers - pending-commit

## Current Task
Awaiting next task

## Learnings
- Manifest verifier parsing is now isolated behind `parseJsonFile(...)` and reports failures with `references/ralph-resource-manifest.v1.json` in the aggregate item.

### Implementation task 4: 1.3 [YELLOW] Refactor manifest verifier helpers (2026-07-02T16:41:30.385Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #4
- Evidence: `npm run prepack` exited 0; commit stat includes only verifier, tasks.md, and progress file.

## Learnings

### Verification: C1 Quality check: prepack after manifest bootstrap
- Status: PASS
- Commands/Tools: `npm run prepack`(0)
- Evidence: Package verifier completed successfully via `node scripts/verify-publish-bundle.mjs`; no `Smart Ralph package verification failed:` output.

### Implementation task 5: [VERIFY] C1 Quality check: prepack after manifest bootstrap (2026-07-02T16:41:39.551Z)
- Agent: ralph-qa-engineer
- Signal: VERIFICATION_PASS
- Native task: #5
- Evidence: `npm run prepack`: PASS exited 0; verifier completed with no failure output.

### Implementation task 6: 1.4 [RED] Failing verifier: manifest entries must use the v1 shape (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS`
- Evidence: RED_PASS; verifier now rejects an invalid manifest placeholder entry with invalid `originalPath`, `piPath`, `kind`, and missing non-copied `notes`.

## Completed Tasks
- [x] 1.4 [RED] Failing verifier: manifest entries must use the v1 shape - HEAD

## Current Task
Awaiting next task

## Learnings
- `RalphResourceManifestV1` entry validation now enforces string paths, finite kind/status sets, optional string metadata, and non-empty notes for non-`copied` statuses.

### Implementation task 6: 1.4 [RED] Failing verifier: manifest entries must use the v1 shape (2026-07-02T16:43:37.065Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #6
- Evidence: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS` output `RED_PASS`

### Implementation task 7: 1.5 [GREEN] Pass v1 shape validation with a valid seed entry (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run prepack`
- Evidence: prepack exited 0 after replacing the invalid manifest placeholder with a valid deferred `commands/ralph-start.md` seed entry.

## Completed Tasks
- [x] 1.5 [GREEN] Pass v1 shape validation with a valid seed entry - pending-commit

## Current Task
Awaiting next task

## Learnings
- A deferred seed entry with source-root-relative `originalPath`, empty `piPath`, allowed `kind`/`status`, and non-empty `notes` satisfies current `RalphResourceManifestV1` shape validation.

### Implementation task 7: 1.5 [GREEN] Pass v1 shape validation with a valid seed entry (2026-07-02T16:45:14.443Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #7
- Evidence: `npm run prepack` exited 0 after valid deferred seed entry.

### Implementation task 8: 1.6 [YELLOW] Improve manifest validation diagnostics (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run prepack`
- Evidence: prepack exited 0 after adding manifest entry diagnostic labels with indexes and `originalPath` values while preserving aggregate failure reporting.

## Completed Tasks
- [x] 1.6 [YELLOW] Improve manifest validation diagnostics - HEAD

## Current Task
Awaiting next task

## Learnings
- Manifest entry validation failures now use labels like `references/ralph-resource-manifest.v1.json[0] originalPath="commands/ralph-start.md"` so aggregate verifier output identifies both the entry index and source resource path.

### Implementation task 8: 1.6 [YELLOW] Improve manifest validation diagnostics (2026-07-02T16:46:41.489Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #8
- Evidence: `npm run prepack` exited 0.

### Implementation task 9: 2.1 [RED] Failing verifier: templates root must contain package files (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS`
- Evidence: RED_PASS; full verifier output includes `templates/ must contain at least one packaged resource file besides .gitkeep` while `templates/` contains only `.gitkeep`.

## Completed Tasks
- [x] 2.1 [RED] Failing verifier: templates root must contain package files - HEAD

## Current Task
Awaiting next task

## Learnings
- Package resource root validation now recursively treats `.gitkeep` as non-content and reports `templates/` explicitly when no packaged template resource exists.

### Implementation task 9: 2.1 [RED] Failing verifier: templates root must contain package files (2026-07-02T16:47:48.003Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #9
- Evidence: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS` output `RED_PASS`; full failure mentions `templates/`.

### Implementation task 10: 2.2 [GREEN] Populate packaged templates from original resources (2026-07-03)
- Status: PASS
- Commands/Tools: `find agents extensions prompts references skills templates -maxdepth 3 -type f | sort && npm run prepack`
- Evidence: command listed copied non-prompt template markdown under `templates/` and `npm run prepack` exited 0.

## Completed Tasks
- [x] 2.2 [GREEN] Populate packaged templates from original resources - HEAD

## Current Task
Awaiting next task

## Learnings
- Non-prompt original template markdown files copied directly from the Ralph Specum template root are sufficient to satisfy the current packaged `templates/` resource-root prepack check while leaving `templates/prompts/*.md` for the prompts task.

### Implementation task 10: 2.2 [GREEN] Populate packaged templates from original resources (2026-07-02T16:49:14.312Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #10
- Evidence: `find agents extensions prompts references skills templates -maxdepth 3 -type f | sort && npm run prepack` listed packaged templates and prepack exited 0.

### Implementation task 11: 2.3 [RED] Failing verifier: prompts root must contain package files (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS`
- Evidence: RED_PASS; full verifier output includes `prompts/ must contain at least one packaged resource file besides .gitkeep` while `prompts/` contains only `.gitkeep`.

## Completed Tasks
- [x] 2.3 [RED] Failing verifier: prompts root must contain package files - HEAD

## Current Task
Awaiting next task

## Learnings
- Package resource root validation now also reports `prompts/` explicitly when the Pi prompt root has no non-`.gitkeep` packaged files.

### Implementation task 11: 2.3 [RED] Failing verifier: prompts root must contain package files (2026-07-02T16:49:14Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #11
- Evidence: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS` output `RED_PASS`; full failure mentions `prompts/`.

### Implementation task 11: 2.3 [RED] Failing verifier: prompts root must contain package files (2026-07-02T16:50:43.649Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #11
- Evidence: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS` output `RED_PASS`; verifier failure mentions `prompts/`.

### Implementation task 12: 2.4 [GREEN] Populate Pi prompt resources (2026-07-03)
- Status: PASS
- Commands/Tools: `find agents extensions prompts references skills templates -maxdepth 3 -type f | sort && npm run prepack`
- Evidence: command listed `prompts/executor-prompt.md` and `prompts/research-prompt.md`; `npm run prepack` exited 0.

## Completed Tasks
- [x] 2.4 [GREEN] Populate Pi prompt resources - HEAD

## Current Task
Awaiting next task

## Learnings
- Original `templates/prompts/executor-prompt.md` and `research-prompt.md` can be packaged under predictable Pi paths `prompts/executor-prompt.md` and `prompts/research-prompt.md` without adding any executable command registration.

### Implementation task 12: 2.4 [GREEN] Populate Pi prompt resources (2026-07-02T16:51:48.965Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #12
- Evidence: `find agents extensions prompts references skills templates -maxdepth 3 -type f | sort && npm run prepack` listed prompt files and prepack exited 0.

## Learnings

### Verification: C2 Quality check: prepack after templates and prompts
- Status: PASS
- Commands/Tools: `npm run prepack`(0)
- Evidence: Package verifier completed successfully via `node scripts/verify-publish-bundle.mjs`; no `Smart Ralph package verification failed:` output.

### Implementation task 13: [VERIFY] C2 Quality check: prepack after templates and prompts (2026-07-02T16:52:03.110Z)
- Agent: ralph-qa-engineer
- Signal: VERIFICATION_PASS
- Native task: #13
- Evidence: `npm run prepack`: PASS exited 0; verifier ran `node scripts/verify-publish-bundle.mjs` with no failure output.

### Implementation task 14: 2.5 [RED] Failing verifier: references root must contain workflow references (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS`
- Evidence: RED_PASS; full verifier output includes missing `references/` packaged resource content beyond `.gitkeep` and the manifest, plus missing `references/original-commands/` directory.

## Completed Tasks
- [x] 2.5 [RED] Failing verifier: references root must contain workflow references - HEAD

## Current Task
Awaiting next task

## Learnings
- References resource-root validation now excludes `.gitkeep` and `references/ralph-resource-manifest.v1.json` from content checks and separately requires `references/original-commands/` to keep original commands packaged as non-executable archives.

### Implementation task 14: 2.5 [RED] Failing verifier: references root must contain workflow references (2026-07-02T16:52:03Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #14
- Evidence: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS` output `RED_PASS`; verifier failure mentions `references/` and `references/original-commands/`.

### Implementation task 14: 2.5 [RED] Failing verifier: references root must contain workflow references (2026-07-02T16:54:27.776Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #14
- Evidence: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS` output `RED_PASS`

### Implementation task 15: 2.6 [GREEN] Populate workflow references and original command archives (2026-07-03)
- Status: PASS
- Commands/Tools: `find agents extensions prompts references skills templates -maxdepth 3 -type f | sort && npm run prepack`
- Evidence: command listed copied workflow references under `references/` and archived original commands under `references/original-commands/`; `npm run prepack` exited 0.

## Completed Tasks
- [x] 2.6 [GREEN] Populate workflow references and original command archives - HEAD

## Current Task
Awaiting next task

## Learnings
- Original command markdown is packaged only under `references/original-commands/`, preserving archive/audit access without placing files in an executable Pi command location.

### Implementation task 15: 2.6 [GREEN] Populate workflow references and original command archives (2026-07-02T16:55:28.645Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #15
- Evidence: `find agents extensions prompts references skills templates -maxdepth 3 -type f | sort && npm run prepack` listed references/original-commands resources and prepack exited 0.

### Implementation task 16: 2.7 [RED] Failing verifier: skills root must contain package files (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS`
- Evidence: RED_PASS; full verifier output includes `skills/ must contain at least one packaged resource file besides .gitkeep` while `skills/` contains only `.gitkeep`.

## Completed Tasks
- [x] 2.7 [RED] Failing verifier: skills root must contain package files - HEAD

## Current Task
Awaiting next task

## Learnings
- Package resource root validation now reports `skills/` explicitly when the Pi skills root has no non-`.gitkeep` packaged files.

### Implementation task 16: 2.7 [RED] Failing verifier: skills root must contain package files (2026-07-02T16:56:39.632Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #16
- Evidence: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS` output `RED_PASS`; full prepack failure mentions `skills/`.

### Implementation task 17: 2.8 [GREEN] Populate Pi-safe skill resources (2026-07-03)
- Status: PASS
- Commands/Tools: `find agents extensions prompts references skills templates -maxdepth 3 -type f | sort && npm run prepack`
- Evidence: command listed copied skill `SKILL.md` files and nested `references/*.md`; `npm run prepack` exited 0.

## Completed Tasks
- [x] 2.8 [GREEN] Populate Pi-safe skill resources - HEAD

## Current Task
Awaiting next task

## Learnings
- Skill resources can be packaged under `skills/<skill>/` while preserving nested reference files; the legacy stop-hook wording was adapted to state Pi coordinator execution and non-invocation of original Claude/Codex hooks.

### Implementation task 17: 2.8 [GREEN] Populate Pi-safe skill resources (2026-07-02T16:58:02.853Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #17
- Evidence: `find agents extensions prompts references skills templates -maxdepth 3 -type f | sort && npm run prepack` listed skill resources and prepack exited 0.

## Learnings

### Verification: C3 Quality check: prepack after references and skills
- Status: PASS
- Commands/Tools: `npm run prepack`(0)
- Evidence: Package verifier completed successfully via `node scripts/verify-publish-bundle.mjs`; no `Smart Ralph package verification failed:` output.

### Implementation task 18: [VERIFY] C3 Quality check: prepack after references and skills (2026-07-02T16:58:10.796Z)
- Agent: ralph-qa-engineer
- Signal: VERIFICATION_PASS
- Native task: #18
- Evidence: npm run prepack: PASS exited 0; ran `node scripts/verify-publish-bundle.mjs` with no failure output.

### Implementation task 19: 2.9 [RED] Failing verifier: schema resource and package allowlist are required (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS`
- Evidence: RED_PASS; full verifier output includes missing `schemas/spec.schema.json` and missing `schemas` in `package.json.files`.

## Completed Tasks
- [x] 2.9 [RED] Failing verifier: schema resource and package allowlist are required - HEAD

## Current Task
Awaiting next task

## Learnings
- Schema packaging now starts with a RED prepack verifier check that fails until both `schemas/spec.schema.json` exists and `package.json.files` includes the `schemas` resource root.

### Implementation task 19: 2.9 [RED] Failing verifier: schema resource and package allowlist are required (2026-07-02T17:00:10.480Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #19
- Evidence: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS` output `RED_PASS`

### Implementation task 20: 2.10 [GREEN] Package Smart Ralph schema and include schemas root (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run prepack`
- Evidence: prepack exited 0 after copying original `schemas/spec.schema.json` and adding `schemas` to `package.json.files`.

## Completed Tasks
- [x] 2.10 [GREEN] Package Smart Ralph schema and include schemas root - HEAD

## Current Task
Awaiting next task

## Learnings
- The original Smart Ralph spec schema can be packaged internally at `schemas/spec.schema.json`; adding the top-level `schemas` root to `package.json.files` satisfies the schema allowlist verifier without adding any `exports` entry.

### Implementation task 20: 2.10 [GREEN] Package Smart Ralph schema and include schemas root (2026-07-02T17:01:44.281Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #20
- Evidence: `npm run prepack` exited 0 after packaging `schemas/spec.schema.json` and adding `schemas` to package files.

### Implementation task 21: 2.11 [YELLOW] Remove stale placeholder files from populated roots (2026-07-03)
- Status: PASS
- Commands/Tools: `find agents extensions prompts references skills templates -maxdepth 3 -type f | sort && npm run prepack`
- Evidence: command output no longer listed `.gitkeep` under prompts/references/skills/templates; `npm run prepack` exited 0.

## Completed Tasks
- [x] 2.11 [YELLOW] Remove stale placeholder files from populated roots - HEAD

## Current Task
Awaiting next task

## Learnings
- Populated resource roots do not require `.gitkeep`; removing the root placeholders keeps the packaged directory tree stable because real files remain in each root.

### Implementation task 21: 2.11 [YELLOW] Remove stale placeholder files from populated roots (2026-07-03)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #21
- Evidence: `find agents extensions prompts references skills templates -maxdepth 3 -type f | sort && npm run prepack` listed no prompts/references/skills/templates `.gitkeep` files and prepack exited 0.

### Implementation task 21: 2.11 [YELLOW] Remove stale placeholder files from populated roots (2026-07-02T17:03:02.747Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #21
- Evidence: `find agents extensions prompts references skills templates -maxdepth 3 -type f | sort && npm run prepack` listed no populated-root `.gitkeep` files and `npm run prepack` exited 0.

### Implementation task 22: 3.1 [RED] Failing verifier: manifest must cover every original resource file (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS`
- Evidence: RED_PASS; full verifier output reports incomplete `references/ralph-resource-manifest.v1.json` original resource coverage and lists missing original resource paths.

## Completed Tasks
- [x] 3.1 [RED] Failing verifier: manifest must cover every original resource file - HEAD

## Current Task
Awaiting next task

## Learnings
- Original resource coverage verification now reads `RALPH_ORIGINAL_RESOURCE_ROOT` or defaults to `/home/nephy/pi-custom-workflow/smart-ralph/plugins/ralph-specum`, enumerating `commands`, `templates`, `references`, `skills`, and `schemas` source-root-relative files.

### Implementation task 22: 3.1 [RED] Failing verifier: manifest must cover every original resource file (2026-07-02T17:04:46.018Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #22
- Evidence: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS` output `RED_PASS`

### Implementation task 23: 3.2 [GREEN] Populate complete RalphResourceManifestV1 coverage (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run prepack`
- Evidence: prepack exited 0 after replacing the seed manifest with 55 entries covering every original command/template/reference/skill/schema file.

## Completed Tasks
- [x] 3.2 [GREEN] Populate complete RalphResourceManifestV1 coverage - HEAD

## Current Task
Awaiting next task

## Learnings
- The complete manifest covers 55 original files: original commands map to `references/original-commands/`, prompt templates map to `prompts/` with `kind: "prompt"`, and no entries require `excluded` or `deferred` status.

### Implementation task 23: 3.2 [GREEN] Populate complete RalphResourceManifestV1 coverage (2026-07-02T17:06:24.895Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #23
- Evidence: `npm run prepack` exited 0 after manifest coverage was populated with 55 original resource entries.

### Implementation task 24: 3.3 [YELLOW] Normalize manifest ordering and kind classification (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run prepack`
- Evidence: prepack exited 0 after sorting manifest entries by `originalPath`; prompt-template entries remain `kind: "prompt"` and command archives remain non-executable references with `kind: "command"`.

## Completed Tasks
- [x] 3.3 [YELLOW] Normalize manifest ordering and kind classification - pending-commit

## Current Task
Awaiting next task

## Learnings
- Manifest ordering is now deterministic by source-root-relative `originalPath`, with original command files archived under `references/original-commands/` rather than executable command paths.

### Implementation task 24: 3.3 [YELLOW] Normalize manifest ordering and kind classification (2026-07-02T17:08:15.017Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #24
- Evidence: `npm run prepack` exited 0 after manifest ordering/classification normalization.

## Learnings

### Verification: C4 Quality check: prepack after manifest coverage
- Status: PASS
- Commands/Tools: `npm run prepack`(0)
- Evidence: Package verifier completed successfully via `node scripts/verify-publish-bundle.mjs`; no `Smart Ralph package verification failed:` output.

### Implementation task 25: [VERIFY] C4 Quality check: prepack after manifest coverage (2026-07-02T17:08:27.203Z)
- Agent: ralph-qa-engineer
- Signal: VERIFICATION_PASS
- Native task: #25
- Evidence: `npm run prepack`: PASS exited 0; ran `node scripts/verify-publish-bundle.mjs` with no failure output.

### Implementation task 26: 3.4 [RED] Failing verifier: piPath existence, notes, and checksums must be enforced (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS`
- Evidence: RED_PASS; full verifier output reports missing SHA-256 digest data for packaged manifest entries.

## Completed Tasks
- [x] 3.4 [RED] Failing verifier: piPath existence, notes, and checksums must be enforced - pending-commit

## Current Task
Awaiting next task

## Learnings
- Manifest integrity validation now requires non-empty `piPath` entries to exist, packaged entries to carry lowercase SHA-256 digests matching the Pi file, and `copied`/`renamed` claims to byte-match the original resource file.

### Implementation task 26: 3.4 [RED] Failing verifier: piPath existence, notes, and checksums must be enforced (2026-07-03)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #26
- Evidence: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS` output `RED_PASS`; full prepack failure reports missing SHA-256 digest data for manifest packaged entries.

### Implementation task 26: 3.4 [RED] Failing verifier: piPath existence, notes, and checksums must be enforced (2026-07-02T17:10:21.888Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #26
- Evidence: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS` output `RED_PASS`

### Implementation task 27: 3.5 [GREEN] Add checksum data and fix integrity failures (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run prepack`
- Evidence: prepack exited 0 after adding SHA-256 metadata for every packaged manifest entry and preserving exact-copy validation for copied/renamed resources.

## Completed Tasks
- [x] 3.5 [GREEN] Add checksum data and fix integrity failures - HEAD

## Current Task
Awaiting next task

## Learnings
- Manifest integrity now records packaged-file SHA-256 digests for all non-empty `piPath` entries; `npm run prepack` verifies digest accuracy and byte-identical source matches for `copied`/`renamed` statuses.

### Implementation task 27: 3.5 [GREEN] Add checksum data and fix integrity failures (2026-07-02T17:12:06.093Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #27
- Evidence: `npm run prepack` exited 0 after adding manifest SHA-256 integrity metadata.

### Implementation task 28: 3.6 [YELLOW] Refactor checksum and path normalization helpers (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run prepack && npm run prepack`
- Evidence: both consecutive prepack runs exited 0 after extracting POSIX path normalization and checksum comparison helpers.

## Completed Tasks
- [x] 3.6 [YELLOW] Refactor checksum and path normalization helpers - pending-commit

## Current Task
Awaiting next task

## Learnings
- Manifest integrity validation now normalizes manifest paths to POSIX separators before coverage, package-file resolution, and original-file checksum comparisons while keeping repeated prepack runs deterministic.

### Implementation task 28: 3.6 [YELLOW] Refactor checksum and path normalization helpers (2026-07-02T17:14:19.180Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #28
- Evidence: `npm run prepack && npm run prepack` exited 0 twice.

### Implementation task 29: 4.1 [RED] Failing verifier: package files list must be asset-consistent (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS`
- Evidence: RED_PASS; full verifier output reports absent `LICENSE` and `smart-ralph.png` entries still listed in `package.json.files`.

## Completed Tasks
- [x] 4.1 [RED] Failing verifier: package files list must be asset-consistent - HEAD

## Current Task
Awaiting next task

## Learnings
- Package file allowlist consistency now fails prepack when `package.json.files` lists absent root assets such as `LICENSE` or `smart-ralph.png`, while also enforcing declared resource roots remain listed.

### Implementation task 29: 4.1 [RED] Failing verifier: package files list must be asset-consistent (2026-07-02T17:15:54.939Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #29
- Evidence: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS` output `RED_PASS`

### Implementation task 30: 4.2 [GREEN] Resolve asset consistency by removing absent file entries (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run prepack`
- Evidence: prepack exited 0 after removing absent `LICENSE` and `smart-ralph.png` from `package.json.files` while keeping resource roots including `schemas`.

## Completed Tasks
- [x] 4.2 [GREEN] Resolve asset consistency by removing absent file entries - HEAD

## Current Task
Awaiting next task

## Learnings
- Removing absent root asset entries from `package.json.files` satisfies the package allowlist consistency verifier while retaining package resource roots (`agents`, `extensions`, `prompts`, `references`, `skills`, `templates`, `schemas`).

### Implementation task 30: 4.2 [GREEN] Resolve asset consistency by removing absent file entries (2026-07-03)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #30
- Evidence: `npm run prepack` exited 0 after absent asset entries were removed from `package.json.files`.

### Implementation task 30: 4.2 [GREEN] Resolve asset consistency by removing absent file entries (2026-07-02T17:17:07.321Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #30
- Evidence: `npm run prepack` exited 0 after removing absent publish asset entries.

### Implementation task 31: 4.3 [YELLOW] Stabilize package allowlist ordering (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run prepack`
- Evidence: prepack exited 0 after moving `schemas` into sorted package resource-root order before `skills` and `templates`.

## Completed Tasks
- [x] 4.3 [YELLOW] Stabilize package allowlist ordering - HEAD

## Current Task
Awaiting next task

## Learnings
- `package.json.files` now keeps package roots in stable category/alphabetical order: runtime roots (`agents`, `extensions`), resource roots (`prompts`, `references`, `schemas`, `skills`, `templates`), then documentation (`README.md`).

### Implementation task 31: 4.3 [YELLOW] Stabilize package allowlist ordering (2026-07-02T17:18:36.144Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #31
- Evidence: `npm run prepack` exited 0 after package allowlist ordering update.

## Learnings

### Verification: C5 Quality check: prepack after package allowlist changes
- Status: PASS
- Commands/Tools: `npm run prepack`(0)
- Evidence: Package verifier completed successfully via `node scripts/verify-publish-bundle.mjs`; no `Smart Ralph package verification failed:` output.

### Implementation task 32: [VERIFY] C5 Quality check: prepack after package allowlist changes (2026-07-02T17:18:45.988Z)
- Agent: ralph-qa-engineer
- Signal: VERIFICATION_PASS
- Native task: #32
- Evidence: `npm run prepack`: PASS exited 0; ran `node scripts/verify-publish-bundle.mjs` with no failure output.

### Implementation task 33: 4.4 [RED] Failing dry-run verifier entrypoint (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run verify:pack 2>&1 | grep -q "Error\\|Cannot find module\\|MODULE_NOT_FOUND" && echo RED_PASS`
- Evidence: RED_PASS; `verify:pack` now invokes missing `scripts/verify-pack-dry-run.mjs`, proving the dry-run verifier entrypoint is required.

## Completed Tasks
- [x] 4.4 [RED] Failing dry-run verifier entrypoint - HEAD

## Current Task
Awaiting next task

## Learnings
- The package dry-run verifier is now exposed as `npm run verify:pack`; until `scripts/verify-pack-dry-run.mjs` exists, Node reports a missing module and the RED verification fails as intended.

### Implementation task 33: 4.4 [RED] Failing dry-run verifier entrypoint (2026-07-02T17:19:49.076Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #33
- Evidence: `npm run verify:pack 2>&1 | grep -q "Error\|Cannot find module\|MODULE_NOT_FOUND" && echo RED_PASS` output `RED_PASS`

### Implementation task 34: 4.5 [GREEN] Implement machine-readable dry-run pack verifier (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run verify:pack`
- Evidence: verify:pack exited 0 after running and validating `npm pack --dry-run --json` output for required resources and excluded runtime/spec state.

## Completed Tasks
- [x] 4.5 [GREEN] Implement machine-readable dry-run pack verifier - HEAD

## Current Task
Awaiting next task

## Learnings
- The dry-run pack verifier normalizes npm `files[].path` values by stripping an optional `package/` prefix before checking included resources and excluded spec/runtime state.

### Implementation task 34: 4.5 [GREEN] Implement machine-readable dry-run pack verifier (2026-07-02T17:24:11.013Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #34
- Evidence: `npm run verify:pack && echo VERIFY_PACK_PASS` output `VERIFY_PACK_PASS`

### Implementation task 35: 4.6 [YELLOW] Refactor dry-run verifier failure reporting (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run verify:pack && echo VERIFY_PACK_PASS`
- Evidence: VERIFY_PACK_PASS; dry-run verifier now aggregates include/exclude failures with `[included]` and `[excluded]` labels while keeping npm stdout/stderr context limited to npm execution or JSON parse failures.

## Completed Tasks
- [x] 4.6 [YELLOW] Refactor dry-run verifier failure reporting - pending-commit

## Current Task
Awaiting next task

## Learnings
- Dry-run verifier diagnostics now distinguish required included package paths from blocked excluded paths using labeled aggregate failures without printing npm stdout/stderr during ordinary package-boundary validation failures.

### Implementation task 35: 4.6 [YELLOW] Refactor dry-run verifier failure reporting (2026-07-02T17:26:56.664Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #35
- Evidence: `npm run verify:pack && echo VERIFY_PACK_PASS` output `VERIFY_PACK_PASS`

## Learnings

### Verification: C6 Quality check: package dry-run after verifier implementation
- Status: PASS
- Commands/Tools: `npm run prepack && npm run verify:pack`(0)
- Evidence: `npm run prepack` executed `node scripts/verify-publish-bundle.mjs` and `npm run verify:pack` executed `node scripts/verify-pack-dry-run.mjs`; combined command exited 0 with no verifier failure output.

### Implementation task 36: [VERIFY] C6 Quality check: package dry-run after verifier implementation (2026-07-02T17:27:30.555Z)
- Agent: ralph-qa-engineer
- Signal: VERIFICATION_PASS
- Native task: #36
- Evidence: `npm run prepack && npm run verify:pack`: PASS exited 0; both verifier scripts ran with no failure output.

### Implementation task 37: 5.1 [RED] Failing verifier: README must document packaged resources (2026-07-03)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #37
- Evidence: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS` output `RED_PASS`; full prepack failure reports missing README resource contract docs including `schemas/`, manifest path, status names, non-executable hook boundary, and `npm run prepack`.

## Completed Tasks
- [x] 5.1 [RED] Failing verifier: README must document packaged resources - HEAD

## Current Task
Awaiting next task

## Learnings
- README package-resource documentation is now enforced by prepack using the existing aggregate failure report; current docs intentionally fail until they name the resource roots, manifest path/statuses, Pi-native command boundary, and verification commands.

### Implementation task 37: 5.1 [RED] Failing verifier: README must document packaged resources (2026-07-02T17:29:48.599Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #37
- Evidence: `npm run prepack 2>&1 | grep -q "Smart Ralph package verification failed:" && echo RED_PASS` output `RED_PASS`

### Implementation task 38: 5.2 [GREEN] Document resource roots, manifest statuses, and verification commands (2026-07-03)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #38
- Evidence: `npm run prepack` exited 0 after README documented `schemas/`, `references/ralph-resource-manifest.v1.json`, manifest statuses, and package verification commands.

## Completed Tasks
- [x] 5.2 [GREEN] Document resource roots, manifest statuses, and verification commands - pending-commit

## Current Task
Awaiting next task

## Learnings
- README package layout now names the resource parity manifest, schema root, finite manifest statuses, and repository/tarball verification commands used before publishing.

### Implementation task 38: 5.2 [GREEN] Document resource roots, manifest statuses, and verification commands (2026-07-02T17:31:34.284Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #38
- Evidence: `npm run prepack` exited 0 with README resource contract documentation present.

### Implementation task 39: 5.3 [YELLOW] Clarify Pi-native command boundary in docs (2026-07-03)
- Status: PASS
- Commands/Tools: `npm run prepack`
- Evidence: prepack exited 0 after README clarified that Pi commands stay in `extensions/ralph-specum/index.ts`, original command/hook files are non-executable references, and `references/original-commands/` is cross-referenced with the manifest.

## Completed Tasks
- [x] 5.3 [YELLOW] Clarify Pi-native command boundary in docs - HEAD

## Current Task
Awaiting next task

## Learnings
- README now distinguishes executable Pi command handlers from packaged original command/hook references by pointing maintainers to `references/original-commands/` and the resource manifest status record.

### Implementation task 39: 5.3 [YELLOW] Clarify Pi-native command boundary in docs (2026-07-02T17:32:46.423Z)
- Agent: ralph-spec-executor
- Signal: TASK_COMPLETE
- Native task: #39
- Evidence: `npm run prepack` exited 0.
