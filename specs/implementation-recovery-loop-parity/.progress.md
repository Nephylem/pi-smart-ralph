---
spec: implementation-recovery-loop-parity
basePath: ./specs/implementation-recovery-loop-parity
phase: planned
task: 0/0
updated: 2026-07-02T12:54:35.204Z
---

# Progress: implementation-recovery-loop-parity

## Original Goal
As a developer running autonomous implementation, I want Pi /ralph-implement to match original recovery, modification, verification, and completion behavior, so that long task lists finish safely or stop with actionable state.

## Epic Context
- Epic: smart-ralph-parity-audit
- Epic state: ./specs/_epics/smart-ralph-parity-audit/.epic-state.json
- Dependencies: packaged-resource-parity, indexing-command-parity, triage-github-sync-parity

## Completed Tasks
- [x] 1.1 Failing test: execution state boots and resumes with parity fields intact - pending

## Current Task
Awaiting next task

## Learnings
- Current Pi `/ralph-implement` already does single-task delegation, native task-card sync, task modification handling, contradiction/evidence validation, and epic completion updates — source: `extensions/ralph-specum/index.ts`
- Recovery-mode parity is still incomplete: runtime parses `--recovery-mode`, but no live `fixTaskMap`/fix-task orchestration path was found in execution code — source: `extensions/ralph-specum/index.ts`; `references/failure-recovery.md`
- `[P]` markers currently affect task metadata and dependency barriers, but the main loop still executes one task at a time — source: `extensions/ralph-specum/index.ts`; `references/coordinator-pattern.md`
- Completion currently reports `ALL_TASKS_COMPLETE` and updates epic state, but does not delete `.ralph-state.json` or run original cleanup/index/PR finalization steps — source: `extensions/ralph-specum/index.ts`; `references/original-commands/implement.md`

## Blockers
- None currently

## Next
Run /ralph-start implementation-recovery-loop-parity

### Research review iteration 1 (2026-07-05T01:26:23.163Z)
- Result: REVIEW_PASS
- Reviewer output:
~~~text
## Review: research (Iteration 1)

### Findings
| # | Dimension | Status | Finding |
|---|-----------|--------|---------|
| 1 | Completeness | PASS | `## Executive Summary`, substantive `## Codebase Analysis`, and substantive `## Feasibility Assessment` are present. |
| 2 | Grounding | PASS | External claims cite URLs; codebase claims cite repo paths such as `extensions/ralph-specum/index.ts`, `schemas/spec.schema.json`, and `references/*`; MCP claims include explicit search-evidence strings in `## MCP E2E Candidates`. |
| 3 | Scope | PASS | Content stays focused on implementation recovery loop parity, related dependencies, and verification strategy without unmarked tangents. |

### Summary
- Passed: 3/3 dimensions
- Failed: 0/3 dimensions
- Critical issues: None

### Feedback for Revision
No issues found.

REVIEW_PASS
~~~

### Research phase (2026-07-05T01:26:23.164Z)
- Generated research.md and validated coordinator output.
- Approval decision: skipped_non_normal.
- Quick/autonomous mode: continuing to /ralph-requirements without an approval prompt.

## Learnings
- Requirement insight: Implementation parity needs durable `ImplementationLoopStateV1` coverage for resume, corrupt-state rejection, recovery metadata, native task remap, and final evidence.
- Requirement insight: `[P]` parity is now scoped to sequential batch execution with preserved downstream barriers and per-task evidence.
- Requirement insight: Layer 3 review cadence is fixed to original checkpoints: phase boundary, every 5th completed task, and final task.
- Scope decision: Keep fixture-driven verifier coverage in scope; browser/E2E tooling, unrelated command rewrites, and true concurrent `[P]` execution stay out of scope.
- Scope decision: Successful completion must delete `.ralph-state.json`, clean stale `.progress-task-*.md`, require index finalization success, and optionally print PR URL.

### Requirements review iteration 1 (2026-07-05T01:28:10.907Z)
- Result: REVIEW_FAIL
- Error: Reviewer reported REVIEW_FAIL.
- Reviewer output:
~~~text
## Review: requirements (Iteration 1)

### Findings
| # | Dimension | Status | Finding |
|---|-----------|--------|---------|
| 1 | Completeness | PASS | User stories include acceptance criteria, and every functional requirement has a priority. |
| 2 | Testability | FAIL | Several acceptance criteria are not yet specific/measurable because core behavior is left undecided. Evidence: `## Assumptions` says “Final approval will decide whether [P] parity requires true concurrent execution...” and “Final approval will decide whether completion deletes .ralph-state.json or archives equivalent terminal data...”; `AC-3.5` requires Layer 3 review at “defined checkpoints” but no checkpoints are defined; `AC-4.3`/`AC-4.4` depend on an “approved parity contract” that is not specified. These cannot be automated as written. |
| 3 | Traceability | PASS | FRs trace cleanly to user stories and acceptance criteria via the `Trace` and `Acceptance Criteria` columns. |
| 4 | Scope | PASS | Requirements stay focused on `/ralph-implement` recovery, verification, native task sync, and completion behavior, with out-of-scope items explicitly listed. |

### Summary
- Passed: 3/4 dimensions
- Failed: 1/4 dimensions
- Critical issues: Undefined parity decisions leave multiple acceptance criteria non-testable.

### Feedback for Revision
1. Replace the open `[P]` execution decision with a normative requirement: either require true concurrency or require a sequential fallback with exact barrier/evidence semantics.
2. Define the exact Layer 3 review cadence for `AC-3.5` (for example: every completed task, every N tasks, phase boundaries, and/or final task).
3. Convert “approved parity contract” placeholders in `AC-4.3` and `AC-4.4` into explicit expected behaviors for state cleanup/archive and index finalization conditions.
4. Move remaining decision items from `## Unresolved Questions` into concrete, testable acceptance criteria before design begins.

REVIEW_FAIL
~~~

### Requirements review iteration 2 (2026-07-05T01:30:22.855Z)
- Result: REVIEW_PASS
- Reviewer output:
~~~text
## Review: requirements (Iteration 2)

### Findings
| # | Dimension | Status | Finding |
|---|-----------|--------|---------|
| 1 | Completeness | PASS | User stories include acceptance criteria, every functional requirement has a priority, and supporting sections (`Non-Functional Requirements`, `Out of Scope`, `Dependencies`, `Success Criteria`) are present. |
| 2 | Testability | PASS | Previously FAIL, now PASS. The prior open decisions are now resolved into measurable criteria: `AC-3.4` defines `[P]` handling as a sequential batch; `AC-3.5` defines exact Layer 3 checkpoints; `AC-4.3` and `AC-4.4` define explicit cleanup and index-finalization behavior; `## Unresolved Questions` now says `None.` |
| 3 | Traceability | PASS | FRs trace to user stories and specific acceptance criteria via the `Trace` and `Acceptance Criteria` columns. |
| 4 | Scope | PASS | Requirements remain focused on `/ralph-implement` recovery-loop parity and explicitly bound adjacent concerns in `## Out of Scope`. |

### Summary
- Passed: 4/4 dimensions
- Failed: 0/4 dimensions
- Critical issues: None

### Feedback for Revision
No issues found.

REVIEW_PASS
~~~

### Requirements phase (2026-07-05T01:30:22.856Z)
- Generated requirements.md and validated coordinator output.
- Approval decision: skipped_non_normal.
- Quick/autonomous mode: continuing to /ralph-design without an approval prompt.

## Learnings
- Architecture decision: keep `/ralph-implement` command wiring in `extensions/ralph-specum/index.ts`, but move execution-loop policy into `extensions/ralph-specum/implementation-loop.ts` so recovery/finalization logic is importable and fixture-testable.
- Existing pattern to follow: exported helper + case-based verifier structure from `extensions/ralph-specum/task-completion.ts` and `scripts/verify-task-blockers-parity.mjs`.
- Existing pattern to follow: reviewer signal parsing/iteration logging from `extensions/ralph-specum/index.ts` `runArtifactReview` / `appendArtifactReviewProgress`.

### Design review iteration 1 (2026-07-05T01:36:27.874Z)
- Result: REVIEW_PASS
- Reviewer output:
~~~text
## Review: design (Iteration 1)

### Findings
| # | Dimension | Status | Finding |
|---|-----------|--------|---------|
| 1 | Completeness | PASS | Required sections are present: `## Architecture`, `## Components`, `## Data Flow`, `## Technical Decisions`, and `## File Structure`. |
| 2 | Consistency | PASS | Components map cleanly to requirements: command shell covers FR-1/3/12, loop core covers FR-1/2/4/6/7/8/9/10/11, finalizer covers FR-12/13/14/15/16, and verifier covers FR-17. |
| 3 | Feasibility | PASS | Referenced existing paths/APIs are real (`extensions/ralph-specum/index.ts`, `extensions/ralph-specum/indexing.ts`, `schemas/spec.schema.json`, `scripts/verify-task-blockers-parity.mjs`), and new work is explicitly marked as `Create` where needed. |
| 4 | Patterns | PASS | Design follows existing repo conventions by keeping `/ralph-implement` in `extensions/ralph-specum/index.ts`, extracting helper logic into a module, reusing `runRalphIndex`, and citing existing verifier/module patterns in `## Existing Patterns to Follow`. |
| 5 | Principles | PASS | The thin command shell plus extracted loop helper is a simple, low-abstraction split aligned with KISS/DRY and avoids expanding the already-large `index.ts` further. |
| 6 | Holistic Awareness | PASS | Cross-cutting concerns are addressed in `## Error Handling`, `## Edge Cases`, `## Test Strategy`, `## Performance Considerations`, and `## Security Considerations`, including resume safety, cleanup ordering, index failure handling, and related-spec contracts. |

### Summary
- Passed: 6/6 dimensions
- Failed: 0/6 dimensions
- Critical issues: None

### Feedback for Revision
No issues found.

REVIEW_PASS
~~~

### Design phase (2026-07-05T01:36:27.875Z)
- Generated design.md and validated coordinator output.
- Approval decision: skipped_non_normal.
- Quick/autonomous mode: continuing to /ralph-tasks without an approval prompt.

## Learnings
- Planning decision: Intent Classification was missing, so tasks assume `MID_SIZED` non-greenfield parity work and use TDD Red-Green-Yellow sequencing.
- Planning decision: Center the plan on one dedicated fixture-driven verifier, `scripts/verify-implementation-loop-parity.mjs`, extended incrementally per behavior.
- Verification command/tool: `npm run prepack` / research `Quality Commands` row from `package.json`.
- Verification command/tool: `npm run verify:index` and `npm run verify:pack` / research `Quality Commands` rows from `package.json`.
- Verification command/tool: `node scripts/verify-implementation-loop-parity.mjs --case acceptance-checklist` / research verifier-script pattern rows (`scripts/verify-start-flow-parity.mjs`, `scripts/verify-index-parity.mjs --case acceptance-checklist`).

### Tasks review iteration 1 (2026-07-05T01:45:11.546Z)
- Result: REVIEW_FAIL
- Error: Reviewer reported REVIEW_FAIL.
- Reviewer output:
~~~text
## Review: tasks (Iteration 1)

### Findings
| # | Dimension | Status | Finding |
|---|-----------|--------|---------|
| 1 | Completeness | PASS | Tasks consistently include `Do`, `Files`, `Done when`, `Verify`, and `Commit`, and the plan includes implementation, VE, and final gate phases. |
| 2 | Traceability | FAIL | FR-1 / AC-1.2 are not fully covered in the bootstrap tasks. In `tasks.md:8`, the `state-resume` case only preserves `taskIndex`, `taskIteration`, `globalIteration`, `recoveryMode`, `fixTaskMap`, `modificationMap`, `nativeTaskMap`, and `evidence`, but omits required in-flight contract fields such as `phase`, `totalTasks`, `maxFixTasksPerOriginal`, and `maxFixTaskDepth` from `ImplementationLoopStateV1`. Add those fields to the bootstrap verifier and green task scope. |
| 3 | Actionability | PASS | Implementation steps are concrete, file-scoped, and use exact verifier commands/cases. |
| 4 | Structure | PASS | The artifact follows TDD Red/Green/Yellow structure throughout implementation phases and separates VE/final quality gates. |
| 5 | Quality Gates | PASS | `[VERIFY]` checkpoints appear after each 2–3 implementation tasks and final quality gates are present. |
| 6 | Holistic Awareness | FAIL | Cross-spec/contract impact is not acknowledged in the task plan. `tasks.md` has no references to consumed contracts or related specs (`StartCompatibilityContractV1`, `IndexArtifactContractV1`, `RalphResourceManifestV1`; grep found no matches), despite the coordinator state marking `start-and-new-flow-parity`, `indexing-command-parity`, `packaged-resource-parity`, and `spec-refactor-command-parity` as related and `mayNeedUpdate: true`. Add a task or verify gate to confirm compatibility/update needs for those shared contracts and adjacent specs. |

### Summary
- Passed: 4/6 dimensions
- Failed: 2/6 dimensions
- Critical issues: Missing full FR-1/AC-1.2 state-field coverage in bootstrap tasks; no explicit cross-spec contract impact task.

### Feedback for Revision
1. Expand the `state-resume` verifier/task scope to assert all required `ImplementationLoopStateV1` bootstrap fields, including `phase`, `totalTasks`, `maxFixTasksPerOriginal`, and `maxFixTaskDepth`.
2. Add a task or `[VERIFY]` gate for related-spec/contract impact review covering `StartCompatibilityContractV1`, `IndexArtifactContractV1`, `RalphResourceManifestV1`, and refactor-loop expectations when implementation-loop state/finalization behavior changes.

REVIEW_FAIL
~~~

## Learnings
- Planning revision: expanded `state-resume` coverage to include `phase`, `totalTasks`, `maxFixTasksPerOriginal`, and `maxFixTaskDepth` in bootstrap/resume tasks.
- Planning revision: added explicit related-contract coverage for `StartCompatibilityContractV1`, `IndexArtifactContractV1`, `RalphResourceManifestV1`, and `spec-refactor-command-parity` expectations in Phase 3 contract wiring tasks.
- Verification command/tool: `node scripts/verify-implementation-loop-parity.mjs --case contract-wiring` / research verifier-script pattern rows plus `Verification Tooling` source-inspection rows.
- RED verifier insight: current `/ralph-implement` bootstrap lives in `extensions/ralph-specum/index.ts`, lacks `implementation-loop.ts`, and does not yet persist/preserve `maxFixTasksPerOriginal`, `maxFixTaskDepth`, `fixTaskMap`, `nativeTaskMap`, or canonical `evidence` fields for execution resume parity.

### Tasks review iteration 2 (2026-07-05T01:48:20.962Z)
- Result: REVIEW_PASS
- Reviewer output:
~~~text
## Review: tasks (Iteration 2)

### Findings
| # | Dimension | Status | Finding |
|---|-----------|--------|---------|
| 1 | Completeness | PASS | All tasks include `Do`, `Files`, `Done when`, `Verify`, and `Commit`, with phased implementation, VE, and final quality gates present. |
| 2 | Traceability | PASS | Previously FAIL, now PASS. `tasks.md` 1.1-1.2 now explicitly covers full `ImplementationLoopStateV1` bootstrap/resume fields including `phase`, `totalTasks`, `maxFixTasksPerOriginal`, and `maxFixTaskDepth`, mapped to `FR-1`, `FR-2`, `AC-1.1`, and `AC-1.2`. |
| 3 | Actionability | PASS | Steps are concrete, path-specific, and use exact verifier/package commands such as `node scripts/verify-implementation-loop-parity.mjs --case ...` and discovered package commands. |
| 4 | Structure | PASS | The plan follows TDD Red/Green/Yellow sequencing across implementation work and separates `[VERIFY]`, VE, and final gates. |
| 5 | Quality Gates | PASS | `[VERIFY]` gates appear after each 2–3 implementation tasks, plus final VE/quality-gate coverage. |
| 6 | Holistic Awareness | PASS | Previously FAIL, now PASS. `tasks.md` 3.4-3.5 and `Q10` explicitly cover `StartCompatibilityContractV1`, `IndexArtifactContractV1`, `RalphResourceManifestV1`, and `spec-refactor-command-parity` delegation expectations, acknowledging related-spec impact and compatibility checks. |

### Summary
- Passed: 6/6 dimensions
- Failed: 0/6 dimensions
- Critical issues: None

### Feedback for Revision
No issues found.

REVIEW_PASS
~~~

### Tasks phase (2026-07-05T01:48:20.967Z)
- Generated tasks.md and validated coordinator output.
- Approval decision: skipped_non_normal.
- Quick/autonomous mode: continuing to /ralph-implement without an approval prompt.
